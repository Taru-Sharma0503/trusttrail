export type AnomalyResult = { flagged: boolean; severity: 'LOW'|'MEDIUM'|'HIGH'; score: number; reasons: string[] };
export function detectAnomaly(input: { amount: number; allocationCap: number; spentAmount: number; historicalAmounts: number[]; recentLargeCount: number; recipientPreviouslyUsed: boolean }): AnomalyResult { const reasons: string[] = []; let score = 0; const avg = input.historicalAmounts.length ? input.historicalAmounts.reduce((a,b)=>a+b,0) / input.historicalAmounts.length : 0; const remaining = Math.max(0, input.allocationCap - input.spentAmount); if (avg > 0 && input.amount > avg * 3) { score += 45; reasons.push('Withdrawal exceeds three times the category historical average.'); } if (remaining > 0 && input.amount > remaining * .5) { score += 35; reasons.push('Withdrawal exceeds 50% of the remaining category allocation.'); } if (!input.recipientPreviouslyUsed) { score += 20; reasons.push('Recipient wallet has not previously received funds from this campaign.'); } if (input.recentLargeCount >= 2) { score += 25; reasons.push('Multiple unusually large withdrawals were requested in a short period.'); } const severity = score >= 70 ? 'HIGH' : score >= 35 ? 'MEDIUM' : 'LOW'; return { flagged: score > 0, severity, score, reasons }; }
