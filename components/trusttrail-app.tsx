'use client'

import { useMemo, useState } from 'react'
import { AlertTriangle, ArrowRight, BadgeCheck, BarChart3, Check, ChevronRight, CircleHelp, Copy, ExternalLink, FileCheck2, Filter, HandCoins, Landmark, LockKeyhole, Menu, MoreHorizontal, Search, ShieldCheck, Sparkles, WalletCards, X, Zap } from 'lucide-react'
import { campaigns, budgets, demoTransaction, milestones, money, percent, transactions, type Campaign } from '@/lib/trusttrail-data'

type View = 'home' | 'campaigns' | 'transparency' | 'dashboard' | 'ngo' | 'verify'

function Logo() { return <div className="flex items-center gap-2.5 font-semibold tracking-tight"><span className="grid size-8 place-items-center rounded-xl bg-primary text-primary-foreground shadow-sm"><ShieldCheck size={18} /></span><span className="text-[17px]">Trust<span className="text-primary">Trail</span></span></div> }
function Status({ children, amber = false }: { children: React.ReactNode; amber?: boolean }) { return <span className={`inline-flex items-center gap-1.5 rounded-full px-2.5 py-1 text-xs font-medium ${amber ? 'bg-amber/10 text-amber' : 'bg-primary/10 text-primary'}`}><span className="size-1.5 rounded-full bg-current" />{children}</span> }
function Stat({ label, value, icon: Icon, note }: { label: string; value: string; icon: React.ElementType; note?: string }) { return <div className="rounded-2xl border border-border bg-card p-5"><div className="mb-5 flex items-center justify-between"><span className="text-sm text-muted-foreground">{label}</span><span className="grid size-8 place-items-center rounded-lg bg-secondary text-primary"><Icon size={16} /></span></div><div className="font-mono text-2xl font-semibold tracking-tight">{value}</div>{note && <p className="mt-1 text-xs text-muted-foreground">{note}</p>}</div> }
function Header({ onNavigate, onWallet, onDemo }: { onNavigate: (v: View) => void; onWallet: () => void; onDemo: () => void }) { const [open, setOpen] = useState(false); return <header className="sticky top-0 z-40 border-b border-brand-blue/20 bg-brand-blue text-white shadow-sm"><div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-5 lg:px-8"><button onClick={() => onNavigate('home')} aria-label="TrustTrail home"><Logo /></button><nav className="hidden items-center gap-7 text-sm text-white/80 md:flex"><button onClick={() => onNavigate('campaigns')} className="hover:text-foreground">Explore</button><button onClick={() => document.getElementById('how')?.scrollIntoView({ behavior: 'smooth' })} className="hover:text-foreground">How it works</button><button onClick={() => onNavigate('transparency')} className="hover:text-foreground">Transparency</button><button onClick={() => onNavigate('ngo')} className="hover:text-foreground">For NGOs</button></nav><div className="hidden items-center gap-2 md:flex"><button className="rounded-lg px-3 py-2 text-sm text-white/80 hover:bg-white/10 hover:text-white" onClick={() => onNavigate('dashboard')}>Dashboard</button><button onClick={onWallet} className="rounded-lg border border-white/35 px-3.5 py-2 text-sm font-medium text-white hover:bg-white/10"><WalletCards className="mr-2 inline" size={15} />Connect wallet</button><button onClick={onDemo} className="rounded-lg bg-primary px-3.5 py-2 text-sm font-medium text-primary-foreground shadow-sm hover:bg-primary/90"><Zap className="mr-1.5 inline" size={14} />Launch demo</button></div><button className="rounded-lg p-2 md:hidden" onClick={() => setOpen(!open)} aria-label="Open navigation">{open ? <X size={20} /> : <Menu size={20} />}</button></div>{open && <nav className="flex flex-col gap-1 border-t border-border bg-background p-4 md:hidden"><button className="p-3 text-left" onClick={() => { onNavigate('campaigns'); setOpen(false) }}>Explore campaigns</button><button className="p-3 text-left" onClick={() => { onNavigate('transparency'); setOpen(false) }}>Transparency</button><button className="p-3 text-left" onClick={() => { onNavigate('dashboard'); setOpen(false) }}>My dashboard</button><button className="m-2 rounded-lg bg-primary p-3 text-primary-foreground" onClick={onDemo}>Launch demo</button></nav>}</header> }

function Flow() { return <div className="mt-10 grid grid-cols-2 gap-3 sm:grid-cols-5 lg:grid-cols-5">{[['01', 'Donation', HandCoins], ['02', 'Escrow vault', LockKeyhole], ['03', 'Milestone', Landmark], ['04', 'Verified release', BadgeCheck], ['05', 'Public audit', FileCheck2]].map(([n, label, Icon], i) => <div key={label as string} className="relative flex flex-col gap-3 rounded-2xl border border-border bg-card p-4 shadow-sm"><div className="flex items-center justify-between"><span className="font-mono text-xs text-muted-foreground">{n as string}</span><Icon size={17} className="text-primary" /></div><span className="text-sm font-medium">{label as string}</span>{i < 4 && <ArrowRight className="absolute -right-3 top-1/2 z-10 hidden text-muted-foreground sm:block" size={15} />}</div>)}</div> }
function CampaignCard({ campaign, onView }: { campaign: Campaign; onView: () => void }) { return <article className="overflow-hidden rounded-2xl border border-border bg-card shadow-sm transition hover:-translate-y-0.5 hover:shadow-md"><img src={campaign.image} alt="" className="h-40 w-full object-cover" /><div className="p-5"><div className="mb-3 flex items-start justify-between gap-3"><div><h3 className="font-semibold leading-tight">{campaign.title}</h3><p className="mt-1 text-sm text-muted-foreground">{campaign.ngo} · {campaign.location}</p></div><Status>{campaign.score}/100</Status></div><div className="mb-2 flex justify-between text-sm"><span className="font-mono font-medium">{money(campaign.raised)}</span><span className="text-muted-foreground">of {money(campaign.target)}</span></div><div className="h-2 overflow-hidden rounded-full bg-secondary"><div className="h-full rounded-full bg-primary" style={{ width: `${percent(campaign.raised, campaign.target)}%` }} /></div><div className="mt-3 flex items-center justify-between text-xs text-muted-foreground"><span>{percent(campaign.raised, campaign.target)}% funded · {campaign.donors} donors</span><span>{campaign.milestones} milestones</span></div><button onClick={onView} className="mt-5 flex w-full items-center justify-center gap-1 rounded-lg border border-border py-2.5 text-sm font-medium hover:bg-secondary">View transparency <ChevronRight size={15} /></button></div></article> }

function Home({ navigate, onDemo }: { navigate: (v: View) => void; onDemo: () => void }) { return <><section className="mx-auto max-w-7xl px-5 pb-20 pt-16 lg:px-8 lg:pt-24"><div className="max-w-3xl"><Status>Public by design</Status><h1 className="mt-6 max-w-3xl text-balance text-5xl font-semibold leading-[1.05] tracking-[-0.045em] md:text-7xl">Trust every donation.<br /><span className="text-primary">Verify every rupee.</span></h1><p className="mt-7 max-w-2xl text-pretty text-lg leading-8 text-muted-foreground">TrustTrail uses smart-contract escrow, milestone-based releases, and transparent spending rules to make NGO donations publicly verifiable.</p><div className="mt-8 flex flex-wrap gap-3"><button onClick={() => navigate('campaigns')} className="rounded-lg bg-primary px-5 py-3 text-sm font-medium text-primary-foreground shadow-sm hover:bg-primary/90">Explore campaigns <ArrowRight className="ml-2 inline" size={16} /></button><button onClick={() => document.getElementById('how')?.scrollIntoView({ behavior: 'smooth' })} className="rounded-lg border border-border bg-card px-5 py-3 text-sm font-medium hover:bg-secondary">See how it works</button><button onClick={onDemo} className="rounded-lg px-4 py-3 text-sm font-medium text-primary hover:bg-primary/10"><Zap className="mr-1 inline" size={15} />Launch demo</button></div></div><Flow /></section><section className="border-y border-border bg-secondary/45"><div className="mx-auto grid max-w-7xl grid-cols-2 gap-px px-5 py-5 lg:grid-cols-4 lg:px-8"><Stat label="Tracked on TrustTrail" value="₹24.8M+" icon={BarChart3} note="Across verified campaigns" /><Stat label="Active campaigns" value="128" icon={Landmark} note="Open to public audit" /><Stat label="Donations traced" value="14,392" icon={HandCoins} note="Every rupee accounted for" /><Stat label="Transaction traceability" value="99.8%" icon={BadgeCheck} note="Evidence-backed releases" /></div></section><section id="how" className="mx-auto max-w-7xl px-5 py-20 lg:px-8"><div className="max-w-xl"><p className="text-sm font-medium text-primary">The TrustTrail model</p><h2 className="mt-3 text-3xl font-semibold tracking-tight md:text-4xl">Accountability, built into the flow of funds.</h2></div><div className="mt-10 grid gap-4 md:grid-cols-4">{[['01', 'Donate', 'UPI, card, or wallet. No crypto knowledge required.'], ['02', 'Funds enter escrow', 'Your donation moves into a campaign vault, not a personal wallet.'], ['03', 'Milestones unlock spending', 'Predefined proof and category limits govern each release.'], ['04', 'Everyone can verify', 'Receipts, hashes, and outcomes remain publicly inspectable.']].map(([n, t, d]) => <div key={n} className="rounded-2xl border border-border p-5"><span className="font-mono text-sm text-primary">{n}</span><h3 className="mt-8 font-semibold">{t}</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">{d}</p></div>)}</div><div className="mt-20 flex items-end justify-between"><div><p className="text-sm font-medium text-primary">Open campaigns</p><h2 className="mt-2 text-3xl font-semibold tracking-tight">Give with visibility.</h2></div><button onClick={() => navigate('campaigns')} className="hidden text-sm font-medium text-primary sm:block">View all campaigns <ArrowRight className="ml-1 inline" size={15} /></button></div><div className="mt-7 grid gap-5 md:grid-cols-3">{campaigns.map(c => <CampaignCard key={c.id} campaign={c} onView={() => navigate('transparency')} />)}</div></section></> }

function Campaigns({ navigate }: { navigate: (v: View) => void }) { const [q, setQ] = useState(''); const [filter, setFilter] = useState('All'); const filtered = useMemo(() => campaigns.filter(c => (filter === 'All' || c.category === filter) && `${c.title} ${c.ngo} ${c.category}`.toLowerCase().includes(q.toLowerCase())), [q, filter]); return <main className="mx-auto max-w-7xl px-5 py-12 lg:px-8"><div className="max-w-2xl"><p className="text-sm font-medium text-primary">Campaign explorer</p><h1 className="mt-3 text-4xl font-semibold tracking-tight md:text-5xl">Find a cause you can follow.</h1><p className="mt-4 text-muted-foreground">Every campaign is a public record of intent, progress, and proof.</p></div><div className="mt-9 flex flex-col gap-3 md:flex-row"><label className="flex flex-1 items-center gap-2 rounded-lg border border-border bg-card px-3"><Search size={17} className="text-muted-foreground" /><span className="sr-only">Search campaigns</span><input value={q} onChange={e => setQ(e.target.value)} placeholder="Search campaigns, NGOs, categories" className="w-full bg-transparent py-3 text-sm outline-none" /></label><div className="flex gap-2 overflow-auto">{['All', 'Education', 'Healthcare', 'Disaster Relief'].map(x => <button key={x} onClick={() => setFilter(x)} className={`whitespace-nowrap rounded-lg border px-3.5 py-2 text-sm ${filter === x ? 'border-primary bg-primary text-primary-foreground' : 'border-border bg-card hover:bg-secondary'}`}><Filter className="mr-1.5 inline" size={14} />{x}</button>)}</div></div><div className="mt-8 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{filtered.map(c => <CampaignCard key={c.id} campaign={c} onView={() => navigate('transparency')} />)}</div></main> }

function BudgetCard({ item }: { item: typeof budgets[number] }) { return <div className="rounded-xl border border-border p-4"><div className="flex items-center justify-between"><div className="flex items-center gap-2.5"><span className="size-2.5 rounded-full" style={{ background: item.color }} /><span className="text-sm font-medium">{item.name}</span></div><LockKeyhole size={15} className="text-primary" /></div><div className="mt-4 flex justify-between text-xs text-muted-foreground"><span>Spent <b className="font-mono text-foreground">{money(item.spent)}</b></span><span>Allocated <b className="font-mono text-foreground">{money(item.allocated)}</b></span></div><div className="mt-2 h-2 rounded-full bg-secondary"><div className="h-full rounded-full" style={{ width: `${percent(item.spent, item.allocated)}%`, background: item.color }} /></div><div className="mt-2 flex justify-between text-xs"><span className="text-muted-foreground">Remaining</span><span className="font-mono font-medium text-primary">{money(item.allocated - item.spent)}</span></div><p className="mt-3 border-t border-border pt-3 text-[11px] text-muted-foreground"><LockKeyhole className="mr-1 inline" size={11} /> Contract protected · cannot exceed allocation</p></div> }
function Transparency({ onDonate, onReject }: { onDonate: () => void; onReject: () => void }) { return <main className="mx-auto max-w-7xl px-5 py-10 lg:px-8"><div className="flex flex-col justify-between gap-6 md:flex-row md:items-end"><div><div className="flex flex-wrap items-center gap-2"><Status>Publicly verifiable</Status><span className="text-xs text-muted-foreground">Campaign ID · TT-DEMO-001</span></div><h1 className="mt-4 text-4xl font-semibold tracking-tight md:text-5xl">Emergency Medical Support</h1><p className="mt-2 text-muted-foreground">by <span className="font-medium text-foreground">HealthBridge Foundation</span> · Delhi</p></div><button onClick={onDonate} className="rounded-lg bg-primary px-5 py-3 text-sm font-medium text-primary-foreground">Donate to this campaign</button></div><div className="mt-8 grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><Stat label="Total raised" value="₹7,40,000" icon={HandCoins} /><Stat label="Total spent" value="₹4,20,000" icon={BarChart3} /><Stat label="Remaining escrow" value="₹3,20,000" icon={LockKeyhole} /><Stat label="Transparency score" value="98 / 100" icon={BadgeCheck} note="Excellent evidence coverage" /></div><div className="mt-8 grid gap-5 lg:grid-cols-[1.3fr_.7fr]"><section className="rounded-2xl border border-border bg-card p-5 md:p-7"><div className="flex items-center justify-between"><div><p className="text-sm font-medium text-primary">Follow the money</p><h2 className="mt-1 text-xl font-semibold">Donation to receipt</h2></div><Status>Live demo data</Status></div><div className="mt-8 grid gap-3 md:grid-cols-5">{[['Donations', '₹7.4L', HandCoins], ['Vault', '₹3.2L', LockKeyhole], ['Milestone', '03 active', Landmark], ['Release', '₹4.2L', BadgeCheck], ['Proof', '12 files', FileCheck2]].map(([t, v, I], i) => <div key={t as string} className="relative rounded-xl bg-secondary/70 p-4"><I size={18} className="text-primary" /><p className="mt-4 text-xs text-muted-foreground">{t as string}</p><p className="mt-1 font-mono text-sm font-semibold">{v as string}</p>{i < 4 && <ArrowRight className="absolute -right-3 top-1/2 z-10 hidden text-primary/50 md:block" size={16} />}</div>)}</div><div className="mt-8 border-t border-border pt-6"><div className="flex items-center justify-between"><h3 className="font-semibold">Category budgets</h3><span className="text-xs text-muted-foreground">Enforced by vault contract</span></div><div className="mt-4 grid gap-3 md:grid-cols-3">{budgets.map(x => <BudgetCard key={x.name} item={x} />)}</div></div></section><section className="rounded-2xl border border-border bg-card p-5 md:p-7"><div className="flex items-start justify-between"><div><p className="text-sm font-medium text-primary">Rule-based monitor</p><h2 className="mt-1 text-xl font-semibold">Anomaly monitor</h2></div><AlertTriangle className="text-amber" size={20} /></div><div className="mt-5 rounded-xl border border-amber/30 bg-amber/5 p-4"><div className="flex items-center justify-between"><Status amber>Medium severity</Status><span className="text-xs text-muted-foreground">Awaiting explanation</span></div><h3 className="mt-4 font-semibold">Unusual transaction</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">₹1,80,000 withdrawal is <b className="text-foreground">3.1× larger</b> than typical medical-supply transactions.</p><p className="mt-3 text-sm leading-6 text-muted-foreground">Recipient wallet has no previous transaction history with this campaign.</p><button onClick={onReject} className="mt-5 flex items-center gap-1 text-sm font-medium text-primary">View details <ArrowRight size={15} /></button></div><div className="mt-5 rounded-xl bg-secondary/70 p-4 text-sm"><div className="flex items-center gap-2 font-medium"><CircleHelp size={15} className="text-primary" /> Advisory, not an automatic freeze</div><p className="mt-2 leading-6 text-muted-foreground">Flags explain unusual signals so the public and NGO can review them together.</p></div></section></div><div className="mt-5 grid gap-5 lg:grid-cols-[.8fr_1.2fr]"><section className="rounded-2xl border border-border bg-card p-5 md:p-7"><p className="text-sm font-medium text-primary">Release schedule</p><h2 className="mt-1 text-xl font-semibold">Milestone timeline</h2><div className="mt-6 space-y-5">{milestones.map((m, i) => <div key={m.title} className="relative flex gap-4">{i < milestones.length - 1 && <span className="absolute left-[11px] top-7 h-12 w-px bg-border" />}</div>)}</div>{milestones.map(m => <div key={m.title} className="relative mb-5 flex gap-3"><span className={`z-10 grid size-6 shrink-0 place-items-center rounded-full ${m.status === 'completed' ? 'bg-primary text-primary-foreground' : m.status === 'progress' ? 'border-2 border-primary bg-background text-primary' : 'border border-border bg-background text-muted-foreground'}`}>{m.status === 'completed' ? <Check size={14} /> : m.status === 'progress' ? <span className="size-1.5 rounded-full bg-primary" /> : <span className="size-1.5 rounded-full bg-border" />}</span><div className="min-w-0 flex-1"><div className="flex justify-between gap-3"><h3 className="text-sm font-medium">{m.title}</h3><span className="font-mono text-xs">{money(m.amount)}</span></div><p className="mt-1 text-xs text-muted-foreground">{m.detail}{m.date && ` · ${m.date}`}</p>{m.hash && <p className="mt-2 font-mono text-[11px] text-primary">{m.hash} <Copy className="ml-1 inline" size={11} /></p>}</div></div>)}</section><section className="overflow-hidden rounded-2xl border border-border bg-card"><div className="flex items-center justify-between p-5 md:p-7"><div><p className="text-sm font-medium text-primary">Immutable record</p><h2 className="mt-1 text-xl font-semibold">Public audit trail</h2></div><button className="text-xs font-medium text-primary">View explorer <ExternalLink className="ml-1 inline" size={12} /></button></div><div className="overflow-x-auto"><table className="w-full min-w-[650px] text-left text-sm"><thead className="border-y border-border bg-secondary/50 text-xs text-muted-foreground"><tr>{['Date', 'Type', 'Category', 'Amount', 'Status', 'Hash'].map(x => <th key={x} className="px-5 py-3 font-medium">{x}</th>)}</tr></thead><tbody>{transactions.map(t => <tr key={t.hash} className="border-b border-border last:border-0"><td className="px-5 py-4 text-xs text-muted-foreground">{t.date}</td><td className="px-5 py-4 font-medium">{t.type}</td><td className="px-5 py-4 text-muted-foreground">{t.category}</td><td className="px-5 py-4 font-mono">{money(t.amount)}</td><td className="px-5 py-4"><Status>{t.status}</Status></td><td className="px-5 py-4 font-mono text-xs text-primary">{t.hash} <Copy className="ml-1 inline" size={11} /></td></tr>)}</tbody></table></div></section></div></main> }

function Dashboard({ ngo = false, onReject }: { ngo?: boolean; onReject: () => void }) { return <main className="mx-auto max-w-7xl px-5 py-10 lg:px-8"><div className="flex items-end justify-between"><div><p className="text-sm font-medium text-primary">{ngo ? 'NGO workspace' : 'Donor dashboard'}</p><h1 className="mt-2 text-4xl font-semibold tracking-tight">{ngo ? 'Good morning, HealthBridge.' : 'Your impact, in view.'}</h1><p className="mt-2 text-muted-foreground">{ngo ? 'Manage campaigns, releases, and public accountability.' : 'Follow every donation from your wallet to the people it helps.'}</p></div><button onClick={onReject} className="hidden rounded-lg border border-border px-3 py-2 text-sm font-medium sm:block">{ngo ? 'Create campaign' : 'Download receipts'}</button></div><div className="mt-8 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{(ngo ? [['Total funds', '₹12.8L', Landmark], ['In escrow', '₹4.2L', LockKeyhole], ['Released', '₹8.6L', BadgeCheck], ['Active campaigns', '6', BarChart3]] : [['Total donated', '₹18,500', HandCoins], ['Active campaigns', '4', Landmark], ['Impact delivered', '3 milestones', BadgeCheck], ['Receipts verified', '100%', FileCheck2]]).map(([l, v, I]) => <Stat key={l as string} label={l as string} value={v as string} icon={I as React.ElementType} />)}</div><section className="mt-8 overflow-hidden rounded-2xl border border-border bg-card"><div className="flex items-center justify-between p-5 md:p-7"><div><p className="text-sm font-medium text-primary">{ngo ? 'Campaign management' : 'Donation history'}</p><h2 className="mt-1 text-xl font-semibold">{ngo ? 'Your campaigns' : 'My donations'}</h2></div><MoreHorizontal size={20} className="text-muted-foreground" /></div><div className="overflow-x-auto"><table className="w-full min-w-[620px] text-left text-sm"><thead className="border-y border-border bg-secondary/50 text-xs text-muted-foreground"><tr>{(ngo ? ['Campaign', 'Raised', 'Escrow', 'Spent', 'Milestones', 'Status'] : ['Campaign', 'Amount', 'Date', 'Status', 'Transaction']).map(x => <th key={x} className="px-5 py-3 font-medium">{x}</th>)}</tr></thead><tbody>{(ngo ? campaigns : [{ title: 'Emergency Medical Support', ngo: 'HealthBridge Foundation', raised: 5000, target: 0, donors: 0, milestones: 1, score: 98, id: 'x', location: '', category: '', currentMilestone: '', image: '' }, campaigns[1]]).map((c: any, i) => <tr key={c.id} className="border-b border-border last:border-0"><td className="px-5 py-4"><div className="font-medium">{c.title}</div><div className="mt-1 text-xs text-muted-foreground">{ngo ? c.ngo : 'Campaign vault'}</div></td><td className="px-5 py-4 font-mono">{ngo ? money(c.raised) : money(c.raised)}</td><td className="px-5 py-4 text-muted-foreground">{ngo ? money(c.raised - 320000) : 'Aug 02, 2026'}</td><td className="px-5 py-4">{ngo ? money(420000) : <Status>Verified</Status>}</td><td className="px-5 py-4">{ngo ? `${c.milestones} milestones` : <span className="font-mono text-xs text-primary">0x4b7...2e10</span>}</td>{ngo && <td className="px-5 py-4"><Status>Active</Status></td>}</tr>)}</tbody></table></div></section><div className="mt-5 rounded-2xl border border-primary/20 bg-primary/5 p-5"><div className="flex gap-3"><Sparkles className="mt-0.5 shrink-0 text-primary" size={19} /><div><h3 className="font-semibold">{ngo ? 'One action needs your attention' : 'Your latest donation is moving'}</h3><p className="mt-1 text-sm leading-6 text-muted-foreground">{ngo ? 'Respond to the medium-severity medical supplies anomaly before the next release review.' : 'Your ₹5,000 donation entered the escrow vault and is linked to Milestone 2.'}</p><button onClick={onReject} className="mt-3 text-sm font-medium text-primary">{ngo ? 'Review anomaly' : 'Track your money'} <ArrowRight className="ml-1 inline" size={14} /></button></div></div></div></main> }

function Modal({ type, close, onReject, onSuccess }: { type: 'wallet' | 'donate' | 'reject'; close: () => void; onReject?: () => void; onSuccess?: () => void }) { const [step, setStep] = useState(1); const [amount, setAmount] = useState('5000'); const isReject = type === 'reject'; return <div className="fixed inset-0 z-50 grid place-items-center bg-foreground/30 p-4 backdrop-blur-sm" role="dialog" aria-modal="true"><div className="w-full max-w-md rounded-2xl border border-border bg-card p-6 shadow-xl"><div className="flex items-start justify-between"><div><p className="text-sm font-medium text-primary">{isReject ? 'Smart contract response' : type === 'wallet' ? 'Connect a wallet' : step === 4 ? 'Verified donation' : 'Support this campaign'}</p><h2 className="mt-1 text-xl font-semibold">{isReject ? 'Transaction rejected' : type === 'wallet' ? 'Choose your access' : step === 4 ? 'Donation Verified' : `Step ${step} of 3`}</h2></div><button onClick={close} className="rounded-lg p-1 text-muted-foreground hover:bg-secondary" aria-label="Close dialog"><X size={18} /></button></div>{isReject ? <><div className="mt-6 rounded-xl border border-destructive/30 bg-destructive/5 p-5"><div className="flex items-center gap-2 font-semibold text-destructive"><AlertTriangle size={18} /> Category spending limit exceeded</div><div className="mt-5 grid grid-cols-2 gap-4 text-sm"><div><p className="text-xs text-muted-foreground">Allowed</p><p className="mt-1 font-mono text-lg">₹1,00,000</p></div><div><p className="text-xs text-muted-foreground">Requested</p><p className="mt-1 font-mono text-lg">₹1,50,000</p></div></div><div className="mt-4 border-t border-destructive/20 pt-4 text-sm"><span className="text-muted-foreground">Exceeded by </span><b>₹50,000</b></div></div><div className="mt-5 flex items-start gap-3 rounded-xl bg-secondary p-4"><LockKeyhole className="mt-0.5 text-primary" size={17} /><p className="text-sm leading-6"><b>Blocked by Smart Contract.</b><br /><span className="text-muted-foreground">Medical Supplies has an immutable ₹1,00,000 maximum.</span></p></div><button onClick={close} className="mt-6 w-full rounded-lg bg-primary py-3 text-sm font-medium text-primary-foreground">Understood</button></> : type === 'wallet' ? <div className="mt-6 space-y-2">{['Demo Wallet', 'MetaMask', 'WalletConnect'].map((x, i) => <button key={x} onClick={close} className="flex w-full items-center justify-between rounded-xl border border-border p-4 text-left hover:bg-secondary"><span className="flex items-center gap-3"><span className="grid size-9 place-items-center rounded-lg bg-secondary text-primary"><WalletCards size={17} /></span><span><b className="block text-sm">{x}</b><small className="text-xs text-muted-foreground">{i === 0 ? 'Recommended for this demo' : 'Connect securely'}</small></span></span><ChevronRight size={16} /></button>)}</div> : step === 4 ? <div className="mt-6 text-center"><div className="mx-auto grid size-16 place-items-center rounded-full bg-primary/10 text-primary"><Check size={30} /></div><p className="mt-4 text-sm leading-6 text-muted-foreground">Your donation has entered the campaign&apos;s smart-contract vault.</p><p className="mt-4 rounded-lg bg-secondary p-3 font-mono text-xs text-primary">Demo Transaction · 0xa71...d204</p><button onClick={close} className="mt-5 w-full rounded-lg bg-primary py-3 text-sm font-medium text-primary-foreground">Track your donation</button></div> : <div className="mt-6"><label className="text-sm font-medium">Choose amount</label><div className="mt-3 grid grid-cols-4 gap-2">{['500', '1000', '2500', '5000'].map(x => <button key={x} onClick={() => setAmount(x)} className={`rounded-lg border py-2.5 text-sm ${amount === x ? 'border-primary bg-primary/10 text-primary' : 'border-border'}`}>₹{Number(x).toLocaleString('en-IN')}</button>)}</div><input value={amount} onChange={e => setAmount(e.target.value)} className="mt-3 w-full rounded-lg border border-border bg-background p-3 font-mono text-sm outline-none focus:ring-2 focus:ring-ring" aria-label="Custom donation amount" /><div className="mt-6 rounded-xl bg-secondary p-4 text-sm"><div className="flex justify-between"><span className="text-muted-foreground">Destination</span><b>Campaign escrow vault</b></div><div className="mt-2 flex justify-between"><span className="text-muted-foreground">Payment</span><b>UPI / Card / Wallet</b></div></div><button onClick={async () => { await demoTransaction('donation'); if (step === 3) { onSuccess?.(); setStep(4) } else setStep(step + 1) }} className="mt-6 w-full rounded-lg bg-primary py-3 text-sm font-medium text-primary-foreground">{step === 3 ? 'Confirm ₹' + Number(amount).toLocaleString('en-IN') : 'Continue'} <ArrowRight className="ml-1 inline" size={15} /></button></div>}</div></div> }

export default function TrustTrailApp() { const [view, setView] = useState<View>('home'); const [modal, setModal] = useState<'wallet' | 'donate' | 'reject' | null>(null); const [mobileDemo, setMobileDemo] = useState(false); const navigate = (v: View) => { setView(v); window.scrollTo({ top: 0, behavior: 'smooth' }) }; const launch = () => { setMobileDemo(true); setView('transparency') }; return <div className="min-h-screen bg-background text-foreground"><Header onNavigate={navigate} onWallet={() => setModal('wallet')} onDemo={launch} />{view === 'home' && <Home navigate={navigate} onDemo={launch} />}{view === 'campaigns' && <Campaigns navigate={navigate} />}{view === 'transparency' && <Transparency onDonate={() => setModal('donate')} onReject={() => setModal('reject')} />}{view === 'dashboard' && <Dashboard onReject={() => setModal('reject')} />}{view === 'ngo' && <Dashboard ngo onReject={() => setModal('reject')} />}{view === 'verify' && <Transparency onDonate={() => {}} onReject={() => setModal('reject')} />}{modal && <Modal type={modal} close={() => setModal(null)} onReject={() => {}} />}{mobileDemo && <div className="fixed bottom-5 left-1/2 z-30 flex -translate-x-1/2 items-center gap-3 rounded-full border border-primary/20 bg-card px-4 py-2.5 text-xs shadow-lg"><span className="size-2 animate-pulse rounded-full bg-primary" /> Demo mode active <button onClick={() => setMobileDemo(false)} className="text-muted-foreground"><X size={14} /></button></div>}<footer className="border-t border-border px-5 py-8 lg:px-8"><div className="mx-auto flex max-w-7xl flex-col justify-between gap-4 text-xs text-muted-foreground sm:flex-row"><Logo /><span>Infrastructure for accountable giving · Demo experience</span><span>All blockchain activity shown is simulated.</span></div></footer></div> }
